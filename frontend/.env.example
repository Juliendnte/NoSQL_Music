# Base URL of the backend API (Node/Python + Neo4j), no trailing slash.
VITE_API_BASE_URL=http://localhost:3000/api

# Set to "false" once the backend above is reachable to hit it for real.
# Defaults to true when unset, so the app runs standalone during frontend dev.
VITE_USE_MOCKS=true
