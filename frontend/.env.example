# Base URL of the backend API (FastAPI + Neo4j), no trailing slash.
VITE_API_BASE_URL=http://localhost:8000/api

# Set to "true" to run the UI standalone against the bundled demo dataset
# (useful offline or if the backend/Neo4j isn't reachable, e.g. for a demo).
VITE_USE_MOCKS=false
